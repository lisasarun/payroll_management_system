package project.controller;

// EmployeeController functionality is handled directly in MainApplication
// via AdminController, AttendanceController, PerformanceController, PayrollController.
// This class is retained for compatibility.
public class EmployeeController {

    // Intentionally empty — all employee-facing actions routed through
    // dedicated controllers called from MainApplication.runEmployeeDashboard()
}
